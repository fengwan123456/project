# 复制本文件为 .env 并填入真实值

# SQLite（本地开发）；生产切 PostgreSQL 改 provider + url
DATABASE_URL="file:./dev.db"

# 高德开放平台 Web服务 key（可选，缺失时离线降级）
AMAP_WEB_KEY=""

# 高德 JS API 2.0 key + 安全密钥（可选，缺失时前端渲染 fallback）
NEXT_PUBLIC_AMAP_JS_KEY=""
NEXT_PUBLIC_AMAP_SECURITY_CODE=""
